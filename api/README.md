#Vending Machine API

To run 

```
npm install
cp .env.template .env
npm run migrate
npm run seed
npm start
```


`npm run migrate` and `npm run seed` command initializes
 the database and create the first admin user.
 
 The admin user credentials:
 Email: admin@vending-machine-api.com
 Pass: password
